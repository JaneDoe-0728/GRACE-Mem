"""Benchmark entry points that replay an existing run through Agent Filter.

Both replays reuse a finished run's Retrieved_Context and rerun only what comes
after retrieval, so the two arms get bit-identical retrieval input.
"""
